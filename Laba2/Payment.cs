using System.Windows.Forms;

namespace Laba2
{
    internal class Payment
    {
        /*private void EnableAndVisible(bool enable, bool visible, params Control[] control)
        {
            for (int i = 0; i < control.Length; i++) 
            {
                control[i].Enabled = enable;
                control[i].Visible = visible;
            }
        }
        public void Pay(bool PossibleCardPay, bool PossibleOnlineBuying, bool PossibleOnlineBuyingViaCourier, bool PossibleOnlineBuyingViaCourierCASH, double AverageAmountThings)
        {
            if (AverageAmountThings >= 30)
            {
                
                if (PossibleCardPay)
                    if (PossibleOnlineBuying)
                        if (PossibleOnlineBuyingViaCourier == true || PossibleOnlineBuyingViaCourierCASH == true)
            }
            else
            {
                if (PossibleCardPay)
                    if (PossibleOnlineBuying)
                        if (PossibleOnlineBuyingViaCourier == true || PossibleOnlineBuyingViaCourierCASH == true)
            }
        }*/
    }
}
